export interface IElement {
  id: string;
  number: string;
  name: string;
  dates: string;
  urlLink: string;
  category: string;
  imageUrl: string;
  infoBlurb: string;
}

export interface ICategory {
  id: string;
  name: string;
}
